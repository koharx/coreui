import { type FC } from "react";
import type { AlertContextType, AlertProviderProps } from "./types";
export declare const AlertProvider: FC<AlertProviderProps>;
export declare const useAlert: () => AlertContextType;
